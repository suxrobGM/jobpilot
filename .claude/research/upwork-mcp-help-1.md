## find_jobs

```
Search and view marketplace job postings.

Actions:
- search: Search for jobs matching filters. Params: query (string — broad match across the WHOLE posting, title and description, with semantic expansion, so many results will not name the term at all; prefer title when the user asks for a specific kind of work), title (string — match the JOB TITLE only. This is how to cut noise when the user names a role ("React developer", "bookkeeper"): every returned job carries the words in its title. Terms are ANDed, so each word must appear in the title — use 1-3 key words and put everything else in the other filters, since a long title matches nothing. Case-insensitive and stemmed ("bookkeeper" matches "Bookkeeping"), whole words only ("Reac" matches nothing). Cannot be combined with query, which lets non-matching jobs back in, nor with sort=relevance, which drops the filter entirely — both are refused rather than silently ignored. An unmatched title returns no jobs, which is a real answer, not an error), job_type (fixed/hourly), experience_level (entry_level/intermediate/expert), budget_min (number), budget_max (number) (FIXED-PRICE project budget; each must be greater than 0; budget_min cannot exceed budget_max; omit a bound to leave it open. This does NOT filter hourly jobs — for an hourly rate use rate_min/rate_max), skills (array of string — filter to jobs carrying these Upwork skills, at most 5, ANDed: a job must carry every one. If none carries all of them the search retries without the broadest and says so. Names are matched exactly against the Upwork skill list; a name that does not match is reported and NOT applied, never guessed at. Relay skills_note when present so the user knows which skills were used), category (string — scope to a top-level category, by name ("Web, Mobile & Software Dev") or ontology id. An unknown name is rejected with the valid list rather than ignored), subcategory (string — same, one level down), rate_min (number), rate_max (number) (HOURLY rate per hour, the filter to use when the user says "$50/hr" or "pays at least $30 an hour". Same bounds rules as budget. Pair it with job_type=hourly so fixed-price postings, which carry no rate, are not returned alongside), workload (full_time/part_time/as_needed), duration (less_than_1_month/1_to_3_months/3_to_6_months/more_than_6_months — expected project length; this is distinct from workload, which is hours per week. It constrains HOURLY jobs only: fixed-price jobs are returned regardless of their duration, so combine it with job_type=hourly when the user asks for work of a given length), verified_payment_only (boolean — only jobs from clients with a verified payment method), proposals_min (number), proposals_max (number) (filter by number of proposals already submitted; e.g. proposals_max=5 for jobs with fewer than 5 proposals), client_hires_min (number), client_hires_max (number) (filter by the client's past hire count; e.g. client_hires_min=0, client_hires_max=0 for clients with no hires, or client_hires_min=10 for 10+). Range bounds must be >= 0 and min cannot exceed max; omit a bound to leave it open. Each job carries a client block: total_spent (lifetime spend), total_reviews, rating, verification_status and country. It does NOT carry a hire count — client_hires_min/max filter on a number these rows cannot show, so never report one from a search result; use get on a job for client_record, which has the hires alongside the spend. rating there is the score FREELANCERS gave that client (client_rating_basis in the response says so) — treat a low rating as a warning about the client, not as the client being unsuccessful. sort (recency/relevance/client_total_charge/client_rating — omit for newest-first, except when query is set without title, skills, category or subcategory, where the default is relevance. relevance is REFUSED alongside title, skills, category or subcategory, because upstream drops those filters and returns a page that was never narrowed; the other three keep every filter), previous_clients_only (boolean — only jobs from clients you have worked with before), upwork_now_only (boolean — only Upwork Now jobs, where the client wants to start immediately), timezone (string — filter by the client's timezone, as an IANA name like America/New_York), location (string — filter by the client's location, as a country or region name like "United States" or "Europe"). These two take a fixed vocabulary: a value outside it returns an empty page rather than an error, so when a search using either comes back with no jobs, READ empty_result_note — it says whether the value was rejected and gives the accepted spelling to retry with. limit (integer, 1–10, default 10), cursor (string — omit for the first page; to fetch the next page, repeat the SAME filters with cursor set to the endCursor from the previous response's pageInfo, and only when pageInfo.hasNextPage is true). A client's preferred qualifications are NOT in search results — use get on a specific job to see them. Each result includes proposal_count (proposals submitted so far), applied (true when THIS freelancer already applied — never recommend applying again, and say so instead), upwork_now with upwork_now_expires_at, featured (true when Upwork promotes the posting in the Find Work list — say "featured", never "premium"), and, when the freelancer has worked with the client before, previous_client with that client's company_name and last_contract_title. Hire/invite liveness (total_hired, invites_sent) is NOT in search — use get on a specific job before assuming it is still open. This action has NO DATE FILTER: the signed-in job search takes no date field at all, so do NOT tell the user results were narrowed to a time window here. For a REAL date filter use find_jobs action=smart_search with mode=most_recent, which is backed by the recommendation feed and does honour dates: days_posted for "posted today/this week", or from_date/to_date for an explicit range. Those results are personalized to the freelancer rather than the whole marketplace. On this action, the best available approximation is sort=recency plus each result's created_date/published_date for client-side narrowing. Each result also includes url, a ready-to-use Upwork job link. Optional params are refinements: do not silently invent values. If the user makes a broad request, briefly surface the most relevant available refinements and proceed with only the required params plus context the user already provided. Ask before applying optional filters when the user asks for a selective result such as best, top, cheapest, near me, urgent, or only. After returning results, mention useful refinements the user can apply.
- get: Get full details of a job posting including client company info, work history, connects, and the client's preferred qualifications (returned as preferred_qualifications: e.g. contractor_type, minimum job success score, English level, rising_talent, earnings). When present, surface the preferred qualifications to the user so they know what the client is looking for. The response includes connects_cost (the Connects required to apply), connects_balance, can_apply, and a ready-to-use url when available. client_record is the client's hiring record — hires, active contracts, total spend, feedback score and count, hours; surface it when the user is judging whether the client is worth applying to. When the response carries bid_stats_basis, the bid amounts under activityStat.applicationsBidStats have no unit attached. client_work_history holds the client's 5 latest open and 5 latest closed contracts, in separate open/closed lists (its selection field says so), so never derive totals or a count of open jobs from it — client_record has the totals. Params: id or job_id (string, required — the numeric id from find_jobs action=search, a ~02… ciphertext, or a full Upwork job URL, e.g. https://www.upwork.com/jobs/~0212345 — all accepted directly, no need to extract or reformat the id yourself). If this returns access denied for a marketplace job ID, use get_job_posting action=get_marketplace with the same id.
- smart_search: Personalized job recommendations for the signed-in freelancer, from Upwork's own recommender — the same feeds behind the Find Work page. Prefer this over action=search whenever the user wants work for themselves ("find me jobs", "what should I apply to"); use action=search when they supply their own search terms. Do NOT pass skills from the profile: the recommender already knows the freelancer's profile, history and what they have applied to, and takes no query. Each result includes a ready-to-use url. TWO FEEDS. mode=best_match (the DEFAULT) ranks by fit. mode=most_recent returns the newest postings matched to this freelancer, newest first — not the whole marketplace. Whenever you return best_match results, TELL THE USER that Most Recent is also available and offer it; switch to mode=most_recent as soon as they ask for the newest, latest or just-posted work. The response repeats this in mode_note — relay it. Params: mode (best_match/most_recent), job_type (fixed/hourly), experience_level (entry_level/intermediate/expert), budget_min (number), budget_max (number) (FIXED-PRICE project budget; does NOT filter hourly jobs), rate_min (number), rate_max (number) (HOURLY rate per hour — use this, not budget, when the user says "$50/hr"; pair with job_type=hourly). For both pairs each bound must be greater than 0, min cannot exceed max, and omitting a bound leaves it open. skills (array of string, at most 5 — names are matched exactly against the Upwork skill list; a name that does not match is reported and NOT applied, never guessed at. The feed has no skills filter, so these are matched against the recommended jobs after they come back, NOT sent to Upwork — so a short page does not mean little work exists. Relay skills_note when present), category (string — scope to a top-level category, by name ("Web, Mobile & Software Dev") or ontology id. An unknown name is rejected with the valid list rather than ignored), subcategory (string — same, one level down), workload (full_time/part_time/as_needed), duration (less_than_1_month/1_to_3_months/3_to_6_months/more_than_6_months — expected project length; distinct from workload, which is hours per week. It constrains HOURLY jobs only, so combine it with job_type=hourly when the user asks for work of a given length), verified_payment_only (boolean), upwork_now_only (boolean — only Upwork Now jobs, where the client wants to start immediately. Each result also carries upwork_now and upwork_now_expires_at. action=search takes the same filter and reports the same two fields), days_posted (integer >= 1 — only jobs posted in the last N days; days_posted=1 means "posted today"), from_date / to_date (YYYY-MM-DD, or RFC3339 for finer granularity — an explicit range instead of a rolling window; to_date is inclusive of the whole day, and defaults to now when only from_date is given. Cannot be combined with days_posted). These three are the ONLY working date filters in find_jobs, and they apply to mode=most_recent — the best_match recommender picks its own set and ignores the window, so pair any date request with mode=most_recent. action=search cannot filter by date at all. proposals_min (number), proposals_max (number) (RANGE over how many proposals a job already has — there is no exactly-N filter; e.g. proposals_max=5 for jobs with fewer than 5), client_hires_min (number), client_hires_max (number) (RANGE over the client's past hire count; e.g. client_hires_min=0 with client_hires_max=0 for clients who have never hired, or client_hires_min=10 for 10+). Range bounds must be >= 0 and min cannot exceed max; omit a bound to leave it open. previous_clients_only (boolean — only clients you have worked with), timezone (string), location (string), limit (integer, 1–10, default 10), cursor (string, optional — omit for the first page; to fetch the next page repeat the SAME filters and the SAME mode with cursor set to the next_cursor from the previous response, only when hasMore is true). Each result also carries connect_price (what applying costs) and is_applied — never recommend applying to a job where is_applied is true. Optional params are refinements: do not silently invent values. If the user makes a broad request, briefly surface the most relevant available refinements and proceed with only the required params plus context the user already provided. Ask before applying optional filters when the user asks for a selective result such as best, top, cheapest, near me, urgent, or only. After returning results, mention useful refinements the user can apply.
```

Input schema:

```json
{"allOf":[{"if":{"properties":{"action":{"const":"search"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"cursor":{"description":"Page cursor from prior pageInfo.endCursor","type":"string"},"duration":{"description":"Expected project length. Constrains hourly jobs only — fixed-price jobs are returned regardless of duration, so pair it with job_type=hourly to exclude them","enum":["less_than_1_month","1_to_3_months","3_to_6_months","more_than_6_months"],"type":"string"},"limit":{"default":10,"description":"Max results (1–10, default 10)","maximum":10,"minimum":1,"type":"integer"},"upwork_now_only":{"description":"Only Upwork Now jobs (clients wanting to start immediately)","type":"boolean"}},"type":"object"}}}},{"if":{"properties":{"action":{"const":"get"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"id":{"description":"Job ID from find_jobs search (preferred), a ~02… ciphertext, or a full Upwork job URL — pass any of these as-is. If access denied, try get_job_posting action=get_marketplace","type":"string"},"job_id":{"description":"Alias for id — provide id or job_id","type":"string"}},"type":"object"}}}},{"if":{"properties":{"action":{"const":"smart_search"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"cursor":{"description":"Page cursor — the next_cursor from the previous response","type":"string"},"days_posted":{"description":"Only jobs posted within the last N days (days_posted=1 for \"posted today\"). Applies to mode=most_recent; the best_match recommender selects its own set and ignores the window. Cannot be combined with from_date/to_date","minimum":1,"type":"integer"},"duration":{"description":"Expected project length. Constrains hourly jobs only — fixed-price jobs are returned regardless of duration, so pair it with job_type=hourly to exclude them","enum":["less_than_1_month","1_to_3_months","3_to_6_months","more_than_6_months"],"type":"string"},"from_date":{"description":"Only jobs posted on or after this date — YYYY-MM-DD, or an RFC3339 timestamp for finer granularity. Use with to_date for an explicit range; use days_posted instead for a window ending now. Applies to mode=most_recent","type":"string"},"limit":{"default":10,"description":"Max results (1–10, default 10)","maximum":10,"minimum":1,"type":"integer"},"mode":{"description":"Which feed to read. best_match (default) is Upwork's personalized recommendations ranked by fit; most_recent is the newest postings matched to this freelancer, newest first. Use most_recent when the user asks for the newest, latest or just-posted jobs","enum":["best_match","most_recent"],"type":"string"},"to_date":{"description":"Only jobs posted on or before this date — YYYY-MM-DD (inclusive, so to_date=2026-09-03 includes all of the 3rd), or an RFC3339 timestamp. Defaults to now when only from_date is given. Applies to mode=most_recent","type":"string"},"upwork_now_only":{"description":"Only Upwork Now jobs (clients wanting to start immediately)","type":"boolean"}},"type":"object"}}}}],"properties":{"action":{"description":"The operation to perform","enum":["search","get","smart_search"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: true

## manage_proposals

```
Submit, edit, or withdraw freelancer proposals. Submitting can include a Connects boost bid for a top slot on the client's list. Also accept or decline client job invitations. For client offers, use respond_to_offer. Freelancers (TALENT): cannot initiate a proposal room or send the first message on a proposal — the client must contact you first. To reply, use an existing room from list_freelancer_proposals action=get_room or get_messages action=find_room (context_type=proposal). If no room exists yet, tell the user the client needs to message first.

WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.
Returns a preview for review — call confirm_preview to execute after explicit user approval. Only one pending preview is held per action type at a time: starting this action again before the current preview is confirmed replaces it without executing it, so wait for the user's decision on the pending preview before starting another change of this type.

Actions:
- create: Prepare a proposal draft for a marketplace job. EXISTING INVITE/PROPOSAL CHECK — MANDATORY, NEVER SKIP: before preparing a create draft you MUST call list_freelancer_proposals action=invitations to check whether the freelancer already has an invitation for this job, and action=list to check for an existing (pending, active, or declined) proposal. If an invitation exists, do NOT use create — use accept_invitation instead. Calling create when an invite or prior proposal already exists is rejected by Upwork (error VJ-JA-10) and wastes the user's turn. Returns a preview — the proposal is NOT submitted yet. Preview includes connects_cost (the connects REQUIRED to apply — the job's price, NOT the freelancer's balance), connects_balance (the freelancer's available connects), whether the freelancer can apply, and bid stats (avg/min/max rates from other proposals). bid_stats is a Freelancer Plus feature: when the account is not entitled it is replaced by bid_stats_available:false plus bid_stats_note — relay that note's framing and never estimate the amounts yourself. preview.plan is the account's plan name; mention it only when it explains a limitation the user ran into. Preview also includes a 'boost' block — boosting is a connects BID (not a flat fee) for a top slot on the client's list; Upwork charges it only if you finish in the top 4 or the client engages the proposal before the auction closes. If boost.available is false, do NOT offer boosting at all — it is unavailable (e.g. you do not meet the client's preferred qualifications; see boost.reason). Otherwise: boost.current_top_bids are the ACTUAL competing bids on this job (highest first; empty if no one has boosted) — present those, not any invented numbers. If boost.current_top_bids_available is false the bids could not be fetched — say the current bids are unknown; do NOT say no one has boosted. Offer boosting unless boost.recommendation is 'skip' (e.g. the client already hired). boost.recommended_connects is the smallest bid that secures a top slot (often just 1 connect when few or none have boosted) — note it but let the user pick. Never exceed boost.max_boost_connects — the bid is spent from the same balance the proposal itself costs, so it is your_balance minus connects_cost, not your_balance; apply ONLY the amount the user approves (pass it as boost_connects — a whole number of connects, at least 1). Once submitted the bid cannot be edited or withdrawn through this tool; verify it with list_freelancer_proposals action=get, which reports terms.connectsBid as Upwork stored it. If the preview contains unmet_preferred_qualifications, tell the user which of the client's preferred qualifications their proposal does not meet — this is advisory only and does NOT block submission (they can still submit). Present the full preview to the user and ask for confirmation. ALWAYS state connects_cost (the Connects this application will spend) and connects_balance explicitly when you present the preview — never omit the Connects cost. Offer to strengthen the proposal by attaching the freelancer's relevant portfolio projects and/or certificates: ASK the user whether they want to include any, call get_profile action=list_highlights to list what's available, help pick the most relevant to this job, and pass the chosen ids as certificate_ids / portfolio_project_ids. FILE ATTACHMENTS — MANDATORY, NEVER SKIP: before submitting, you MUST ask the user whether they want to attach any files to the proposal (e.g. a resume/CV, work samples, a brief). Never silently skip this question, even though the field is optional. If they say yes, call start_attachment_upload with context='proposals' FIRST, wait for the upload to complete, and pass the returned file_uid values as the attachments param. If the freelancer lacks connects, an error with a purchase URL is returned. Then call confirm_preview with type='proposal' and the returned preview_id to actually submit. After submission, the proposal status is 'Pending' — to verify, use list_freelancer_proposals action=list with status=pending. Use accept_invitation instead if responding to a client invitation. Params: job_reference (string, required — the numeric jobs[].id from find_jobs action=search, NOT the ~02… ciphertext field), cover_letter (string, required — max 5000 characters; keep it concise, and if the user's text is longer ask them to shorten it rather than sending it truncated), charged_amount (number, required — bid amount; not a string). answers (array of {question, answer}, optional — required only when the job has screening_questions, surfaced in the create preview: answer each and pass them here so they submit with the proposal). boost_connects (integer, optional — whole number of connects BID to boost the proposal's rank; minimum 1, never more than boost.max_boost_connects (your_balance less the proposal's own connects_cost); use boost.recommended_connects from the preview). team_org_id (string, optional — only for an agency with more than one team: the team the proposal is submitted under. Omit it; if the agency has several teams the error names them, and the user picks one). attachments (array of strings, optional — file_uid values from start_attachment_upload with context='proposals'. ALWAYS offer attachments to the user before submitting; never silently skip asking). certificate_ids (array of strings, optional — certificate ids from get_profile list_highlights to highlight on the proposal), portfolio_project_ids (array of strings, optional — portfolio project ids from get_profile list_highlights to highlight). Contractor identity is resolved from your session. Org comes from org_uid on execute_tool (maps to teamOrgId).
  Prior context: before creating, look up recent/relevant prior items when available: call list_freelancer_proposals action=list/get for prior proposals; start with recent Accepted proposals and, for strongest templates, also check Offered/Hired examples; use find_jobs or the target job details for the current job. Use prior items to infer cover letter tone, structure, relevant experience, bid patterns, and job-specific requirements, but do not invent required write params. COVER LETTER — make it authentic and specific, never generic or fake-sounding, and ground every claim in the freelancer's REAL history (get_profile for work history and profile aggregates, list_freelancer_proposals for past Accepted/Hired proposals, and get_profile action=list_highlights for projects and certificates): (1) Weave in the most RELEVANT past jobs — what was delivered and the outcome. If none are directly relevant, still cite strong past jobs as evidence of the freelancer's quality and reliability. (2) Reference concrete, REAL signals of client satisfaction (feedback score, repeat clients, Top Rated status) and the quality of past deliverables — never invent praise, metrics, or clients. (3) Include the freelancer's relevant PROJECTS (portfolio work done outside Upwork jobs) and CERTIFICATIONS: mention them in the letter AND suggest attaching them via portfolio_project_ids / certificate_ids. (4) LANGUAGE: if the job requires a language other than English, provide the SAME cover letter in that language as well (English plus the required language) so the client can read it in their own language. Keep it human, specific, and honest — no boilerplate.
- accept_invitation: Accept a client invitation and submit a proposal. Returns a draft — present it and call confirm_preview with type='proposal_accept_invitation' and the returned preview_id to execute (accepting creates a client-visible proposal). Use this when the freelancer was invited to a job by a client (do NOT use 'create' for invitations — it will fail). Get the invitation_uid from list_freelancer_proposals action=invitations (the 'id' field). FILE ATTACHMENTS — MANDATORY, NEVER SKIP: before confirming, you MUST ask the user whether they want to attach any files (e.g. a resume/CV, work samples). Never silently skip this, even though the field is optional. If yes, call start_attachment_upload with context='proposals' FIRST, wait for the upload to complete, and pass the returned file_uid values as attachments. Params: invitation_uid (string, required), cover_letter (string, optional — max 5000 characters), bid (number, optional — proposed rate/amount), attachments (array of strings, optional — file_uid values from start_attachment_upload with context='proposals'; ALWAYS offer attachments before submitting, never silently skip asking). answers (array of {question, answer}, optional — required only when the invited job has screening questions: answer each and pass them here so they submit with the accepted proposal, exactly as with action=create).  Optional params are user choices: do not silently invent values. Briefly surface relevant optional inputs before the action when they could affect the outcome. If the user does not choose them, proceed with only required params plus context the user already provided. COVER LETTER — make it authentic and specific, never generic or fake-sounding, and ground every claim in the freelancer's REAL history (get_profile for work history and profile aggregates, list_freelancer_proposals for past Accepted/Hired proposals, and get_profile action=list_highlights for projects and certificates): (1) Weave in the most RELEVANT past jobs — what was delivered and the outcome. If none are directly relevant, still cite strong past jobs as evidence of the freelancer's quality and reliability. (2) Reference concrete, REAL signals of client satisfaction (feedback score, repeat clients, Top Rated status) and the quality of past deliverables — never invent praise, metrics, or clients. (3) Include the freelancer's relevant PROJECTS (portfolio work done outside Upwork jobs) and CERTIFICATIONS: mention them in the letter AND suggest attaching them via portfolio_project_ids / certificate_ids. (4) LANGUAGE: if the job requires a language other than English, provide the SAME cover letter in that language as well (English plus the required language) so the client can read it in their own language. Keep it human, specific, and honest — no boilerplate.
- decline_invitation: Decline a client job invitation. Returns a draft — present it and call confirm_preview with type='proposal_decline_invitation' and the returned preview_id to execute (declining notifies the client). Get the invitation_uid from list_freelancer_proposals action=invitations (the 'id' field). Params: invitation_uid (string, required), reason_id (string, optional — canned decline reason ID; required when message is provided), reason_other (string, optional — free-text reason), message (string, optional — message to the client; requires reason_id), block_client (boolean, optional — block the client after declining). Optional params are user choices: do not silently invent values. Briefly surface relevant optional inputs before the action when they could affect the outcome. If the user does not choose them, proceed with only required params plus context the user already provided.
- withdraw: Withdraw a pending proposal. Returns a draft — present it and call confirm_preview with type='proposal_withdraw' and the returned preview_id to execute. The proposal must be in a withdrawable state (e.g. Pending). Params: proposal_id (string, required — the proposal ID), reason_id (string, optional — ID of a canned withdrawal reason), other_reason (string, optional — free-text reason if not using a canned reason), message (string, optional — message to the client about the withdrawal). Optional params are user choices: do not silently invent values. Briefly surface relevant optional inputs before the action when they could affect the outcome. If the user does not choose them, proceed with only required params plus context the user already provided.
- edit_terms: Edit the terms (rate/amount) of a pending proposal. Returns a draft — present it and call confirm_preview with type='proposal_edit_terms' and the returned preview_id to execute. Use for hourly projects and fixed-bid projects without milestones. Params: proposal_id (string, required), charge_rate (string, required — new hourly rate or fixed-price amount, e.g. '25.00'), estimated_duration_id (number, optional — duration metadata item), upfront_payment_percent (number, optional — for fixed-price only, 0 or more), message (string, optional). Optional params are user choices: do not silently invent values. Briefly surface relevant optional inputs before the action when they could affect the outcome. If the user does not choose them, proceed with only required params plus context the user already provided.
- send_proposal: Propose a contract directly to a client you are already in a conversation with (e.g. after they invited you or replied to your proposal). NOT binding — a contract only starts if the client accepts and funds it. Returns a draft; present it and call confirm_preview with type='contract_proposal' to send. The client is derived from the conversation, so you do NOT supply client IDs. Params: room_id (string, required — the conversation/Dash room ID from get_messages), title (string, required — contract title), hourly_rate (string, required — e.g. '55.00'), currency (string, default USD), weekly_limit (number, optional — max weekly hours), description (string, optional).
- acknowledge_policy: Acknowledge the disintermediation compliance policy after the user explicitly confirms they understand. Call this only after showing the policy message and receiving explicit user confirmation. No params.
```

Input schema:

```json
{"allOf":[{"if":{"properties":{"action":{"const":"create"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"answers":{"description":"Answers to the job's screening_questions (from the create preview). Required only when the job has screening questions.","items":{"properties":{"answer":{"description":"The freelancer's answer","type":"string"},"question":{"description":"The screening question text","type":"string"}},"required":["question","answer"],"type":"object"},"type":"array"},"boost_connects":{"description":"Optional whole number of connects to BID for a top slot (minimum 1, never more than the preview's boost.max_boost_connects — the balance left after the proposal's own connects_cost). Omit entirely to submit without boosting","type":"integer"},"charged_amount":{"description":"Bid amount as number (not a string)","type":"number"},"cover_letter":{"description":"Required proposal cover letter","type":"string"},"job_reference":{"description":"The numeric jobs[].id from find_jobs action=search. NOT the ~02… ciphertext field","type":"string"},"team_org_id":{"description":"Agency team the proposal is submitted under. Only for a multi-team agency, which is told which teams exist when the team is missing or wrong","type":"string"}},"required":["job_reference","cover_letter","charged_amount"],"type":"object"}}}}],"properties":{"action":{"description":"The operation to perform","enum":["create","accept_invitation","decline_invitation","withdraw","edit_terms","send_proposal","acknowledge_policy"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: false

## get_preview

```
Retrieve a pending server-stored preview without consuming it.

Actions:
- get: Get a pending preview for review. Params: type (required — the preview type), id (required — the preview_id returned by the creating action).
```

Input schema:

```json
{"allOf":[{"if":{"properties":{"action":{"const":"get"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"id":{"description":"Preview ID returned as preview_id by the creating action","type":"string"},"type":{"description":"Preview type returned by the action that created the preview","enum":["job_posting","job_update","proposal","proposal_accept_invitation","proposal_decline_invitation","proposal_withdraw","proposal_edit_terms","milestone_create","milestone_edit","milestone_delete","milestone_reject","milestone_submit","invitation","offer_decline","offer_request_changes","proposal_decline","contract_proposal","contract_pause","contract_restart","contract_end","profile_update_title","profile_update_overview","profile_update_availability","profile_add_employment","profile_set_skills","profile_update_employment","profile_remove_employment","profile_add_language","profile_update_language","profile_remove_language","profile_add_education","profile_update_education","profile_remove_education","profile_add_other_experience","profile_update_other_experience","profile_remove_other_experience","availability_badge_toggle","profile_boost_create","profile_boost_manage","account_update_company","account_update_location","agency_project_create","agency_project_update","agency_project_reorder","agency_project_delete"],"type":"string"}},"required":["type","id"],"type":"object"}}}}],"properties":{"action":{"description":"The operation to perform","enum":["get"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: true

## confirm_preview

```
Confirm and execute a previously prepared preview for job postings, proposals, invitation accepts, invitations, milestones, profile updates, and offer decline / request-changes. Requires preview_id from the prior preview action. Do NOT use to ACCEPT an offer — accepting is finalized on upwork.com via the finalize_url returned by respond_to_offer accept.

WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.
You MUST present full action details and receive explicit approval before executing. Each write requires separate confirmation even if the user said "approve all".

Actions:
- confirm: Confirm and execute a preview using preview_id only. Params: type (string, required), preview_id (string, required — from the create preview response). Do not pass the returned params; they are loaded from server storage.
```

Input schema:

```json
{"properties":{"action":{"description":"The operation to perform","enum":["confirm"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"properties":{"preview_id":{"description":"Server-issued preview_id from the create preview response. confirm_preview executes stored server params only — do not pass confirm payloads.","type":"string"},"type":{"description":"The entity type. confirm_preview does NOT execute offer ACCEPTANCE (that is finalized on upwork.com via the finalize_url); offer decline and request-changes ARE supported. One of: job_posting, job_update, proposal, proposal_accept_invitation, proposal_decline_invitation, proposal_withdraw, proposal_edit_terms, milestone_create, milestone_edit, milestone_delete, milestone_reject, milestone_submit, invitation, offer_decline, offer_request_changes, proposal_decline, contract_proposal, contract_pause, contract_restart, contract_end, profile_update_title, profile_update_overview, profile_update_availability, profile_add_employment, profile_set_skills, profile_update_employment, profile_remove_employment, profile_add_language, profile_update_language, profile_remove_language, profile_add_education, profile_update_education, profile_remove_education, profile_add_other_experience, profile_update_other_experience, profile_remove_other_experience, availability_badge_toggle, profile_boost_create, profile_boost_manage, account_update_company, account_update_location, agency_project_create, agency_project_update, agency_project_reorder, agency_project_delete","enum":["job_posting","job_update","proposal","proposal_accept_invitation","proposal_decline_invitation","proposal_withdraw","proposal_edit_terms","milestone_create","milestone_edit","milestone_delete","milestone_reject","milestone_submit","invitation","offer_decline","offer_request_changes","proposal_decline","contract_proposal","contract_pause","contract_restart","contract_end","profile_update_title","profile_update_overview","profile_update_availability","profile_add_employment","profile_set_skills","profile_update_employment","profile_remove_employment","profile_add_language","profile_update_language","profile_remove_language","profile_add_education","profile_update_education","profile_remove_education","profile_add_other_experience","profile_update_other_experience","profile_remove_other_experience","availability_badge_toggle","profile_boost_create","profile_boost_manage","account_update_company","account_update_location","agency_project_create","agency_project_update","agency_project_reorder","agency_project_delete"],"type":"string"}},"required":["type","preview_id"],"type":"object"}},"required":["action","params","org_uid"],"type":"object"}
```

read_only: false

## set_tool_mode

```
View or change how MCP tools are presented. full_list shows all tools directly; search_execute uses search_tools/execute_tool indirection.

WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.
You MUST present full action details and receive explicit approval before executing. Each write requires separate confirmation even if the user said "approve all".

Actions:
- get: Get the current tool mode preference. No params required.
- set: Set the tool mode preference. Params: mode (string, required — 'full_list' or 'search_execute'). Takes effect on your next tool call or tools/list request.
```

Input schema:

```json
{"properties":{"action":{"description":"The operation to perform","enum":["get","set"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: false

## set_tool_permission

```
View or change per-tool write confirmation settings. Write tools default to 'always_ask'. Set 'always_allow' to let an agent run a specific tool without asking for confirmation first — useful for automated flows. This applies in search_execute mode only, and it never bypasses the draft-confirm gate: a destructive write still requires a separate confirm_preview with the preview_id.

WRITE OPERATION — REQUIRES EXPLICIT USER CONFIRMATION.
You MUST present full action details and receive explicit approval before executing. Each write requires separate confirmation even if the user said "approve all".

Actions:
- get: Get permission for a tool or all tools. Params: tool_name (string, optional — omit to list all).
- set: Set permission for a tool. Params: tool_name (string, required), permission (string, required — 'always_ask' or 'always_allow').
```

Input schema:

```json
{"properties":{"action":{"description":"The operation to perform","enum":["get","set"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: false

## get_account

```
View account info and organization details.

Actions:
- get_user_details: Get user details. Params: id (string, optional — defaults to current user).
- get_organization: Get current organization, including its teams (child organizations) when it has any. Teams are informational: they are NOT selectable accounts, so a team id must never be passed as org_uid — list_accounts returns the only usable org_uid values. No params.
- list_teams: List the teams you belong to under the selected organization, optionally filtered by what you may do in each. Params: action (string, optional — one of approve_milestone, end_contract, hire, invite_freelancer, post_job, send_offer); with it each team carries allowed true/false. Team ids are team_uid FILTER values for other tools — never an org_uid. whole_organization describes acting company-wide, which is done by omitting team_uid, not by passing the org uid.
- get_company: Get the company details of the organization you are acting as. To read a different account of yours, call again with that org_uid. For a marketplace job's client, use find_jobs action=get with the job ID instead. No params.
```

Input schema:

```json
{"allOf":[{"if":{"properties":{"action":{"const":"list_teams"}},"required":["action"]},"then":{"properties":{"params":{"properties":{"action":{"description":"Optional: report whether each team allows this capability. Omit to list the teams with no permission verdict.","enum":["approve_milestone","end_contract","hire","invite_freelancer","post_job","send_offer"],"type":"string"}},"type":"object"}}}}],"properties":{"action":{"description":"The operation to perform","enum":["get_user_details","get_organization","list_teams","get_company"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: true

## get_freelancer_dashboard

```
Get a consolidated overview of what's new: invitations, offers, messages, matching jobs, contract updates, and connects balance — all in one call.

Actions:
- check: Check all activity sources in parallel. No params required.
```

Input schema:

```json
{"properties":{"action":{"description":"The operation to perform","enum":["check"],"type":"string"},"org_uid":{"description":"Your Upwork org_uid from list_accounts","type":"string"},"params":{"description":"Action-specific parameters (see allOf branches per action when present)","type":"object"}},"required":["action","org_uid"],"type":"object"}
```

read_only: true
