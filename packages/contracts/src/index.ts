// @jobpilot/contracts — shared Zod v4 schemas (wire contract + form validators).
// Single source of truth: imported by the backend (Elysia validators via Standard
// Schema) and by the web app (TanStack Form). Populated in Phase 5.
export {};
