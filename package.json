{
  "name": "jobpilot",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "src/web"
  ],
  "scripts": {
    "dev": "concurrently -k -n terminal,web -c cyan,green \"dotnet watch --project src/JobPilot.Terminal run\" \"bun --cwd=src/web run dev\"",
    "start": "bun run db:setup && bun run dev",
    "db:setup": "bun --cwd=src/web run db:migrate:apply && bun --cwd=src/web run db:seed",
    "build:terminal": "dotnet publish src/JobPilot.Terminal -c Release -o dist/terminal",
    "build:web": "bun --cwd=src/web run build"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "concurrently": "^9.1.0",
    "prettier": "^3.8.3"
  }
}
