[test]
# tsyringe throws at import without the reflect polyfill; tests have no entry point to load it from.
# test-env.ts backfills ci.yml's dummy env so the suite also runs with no .env (fresh clone).
preload = ["reflect-metadata", "./scripts/test-env.ts"]
