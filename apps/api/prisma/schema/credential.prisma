model Credential {
  id        String  @id @default(uuid())
  profileId String
  profile   Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)
  scope     String
  email     String?
  password  String?
  apiKey    String?

  @@unique([profileId, scope])
  @@index([profileId])
}
