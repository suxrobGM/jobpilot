import type { ReactElement } from "react";
import { BoardsPageClient } from "@/components/features/boards/boards-page-client";

export default function BoardsPage(): ReactElement {
  return <BoardsPageClient />;
}
